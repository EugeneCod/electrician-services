import { Slider, Promo } from '../../components/';
import { FC } from 'react';

const Home: FC = () => {
  return (
    <section>
      <Slider />
      <Promo />
    </section>
  );
};

export default Home;
